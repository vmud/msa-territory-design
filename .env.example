# PostgreSQL/PostGIS connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/territory_db

# Individual connection params (used if DATABASE_URL not set)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=territory_db
